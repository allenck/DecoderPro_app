#ifndef CHANNELSTARTEDITOR_H
#define CHANNELSTARTEDITOR_H

#include "sdfmacroeditor.h"

class ChannelStartEditor : public SdfMacroEditor
{
 Q_OBJECT
public:
 //explicit ChannelStartEditor(QWidget *parent = 0);
 /*public*/ ChannelStartEditor(SdfMacro* inst,QWidget *parent = 0);

signals:

public slots:

};

#endif // CHANNELSTARTEDITOR_H
