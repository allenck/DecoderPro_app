#ifndef ENDSOUNDEDITOR_H
#define ENDSOUNDEDITOR_H
#include "sdfmacroeditor.h"

class EndSoundEditor : public SdfMacroEditor
{
 Q_OBJECT
public:
 //explicit EndSoundEditor(QWidget *parent = 0);
 /*public*/ EndSoundEditor(SdfMacro* inst, QWidget *parent = 0);

signals:

public slots:

};

#endif // ENDSOUNDEDITOR_H
