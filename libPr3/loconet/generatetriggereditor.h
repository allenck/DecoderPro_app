#ifndef GENERATETRIGGEREDITOR_H
#define GENERATETRIGGEREDITOR_H

#include "sdfmacroeditor.h"

class GenerateTriggerEditor : public SdfMacroEditor
{
 Q_OBJECT
public:
 //explicit GenerateTriggerEditor(QWidget *parent = 0);
 /*public*/ GenerateTriggerEditor(SdfMacro* inst, QWidget *parent = 0);

signals:

public slots:

};

#endif // GENERATETRIGGEREDITOR_H
